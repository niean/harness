<!-- SUMMARY: {{概述该文件内容的一句话}} -->
# 数据与类型边界

## {{核心数据模型}}

{{核心数据模型名称}}（{{文件路径}}）：{{协议列表}}。字段见 ./21-glossary.md。{{序列化文件中不存储的字段说明}}。

{{状态枚举名称}}（同文件）：{{枚举值列表与含义}}，{{可选字段的向下兼容策略}}。

{{其他关联枚举}}（同文件）：{{枚举值列表与默认值、向后兼容策略}}。

## {{配置与服务模型}}

- {{配置模型名称}}（{{文件路径}}）：{{字段列表与关联说明}}
- {{运行时配置文件名}} 结构见下方；{{配置管理器}}读取，设置页可覆盖

## 协调器输入输出

{{协调器名称}}：输入 {{输入类型}}；进度 {{进度模型与字段}}；完成 {{结果类型}}。

## 磁盘存储结构

```
{{存储根目录}}/{id}/
  metadata.json   -- {{轻量摘要用途}}
  record.json     -- {{全量字段，排除的大对象字段}}
  history.json    -- {{历史记录用途}}
  images/image_0.jpg ... -- {{图片格式与尺寸限制}}
  audio.mp3       -- {{音频文件说明}}
  avatar.jpg      -- {{头像缩略图与尺寸}}
  README.txt

{{其他存储目录}}/
  {{子目录结构与用途}}
```

{{加载与重组方式}}。

{{其他资源文件的存储、降采样与加载规则}}。

## {{历史/事件记录}}

{{历史事件模型：字段列表}}。{{历史容器模型：字段列表}}。{{存储位置与读写方式}}。{{导入导出行为}}。

{{历史Manager的委托关系与数据聚合方式}}。

## 运行时配置文件

```json
{
  {{配置文件结构，包含各服务节的Provider/子配置布局}}
}
```

{{各服务Provider选择机制、API Key存储策略、配置读取优先级}}。

## 导出/导入

```
{{导出目录结构}}
```

{{导入时的冲突处理策略}}。

## UserDefaults

{{通过UserDefaults存储的非敏感配置项列表与键名来源}}。

## 边界约定

- {{UI层不直接构造/解析底层请求响应的规则}}
- {{列表加载策略：metadata分离、分页接口、全量接口使用场景}}
- {{图片加载策略：兼容旧数据与新代码的按需加载}}
