# 数据与类型边界

## 核心数据模型

{{列举项目核心数据模型，含文件路径、关键字段、状态枚举等}}

## 配置结构

{{配置文件结构、配置管理器读取优先级、设置覆盖机制}}

## 协调器输入输出

{{核心协调器/服务的输入参数、进度回报、输出结果类型}}

## 磁盘存储结构

```
{{项目数据存储目录结构}}
```

{{数据加载/重组机制说明}}

## 配置文件

```json
{{配置文件示例结构（脱敏）}}
```

{{配置项说明、多 Provider 切换机制等}}

## 导出/导入

{{导出格式、导入冲突处理策略}}

## 本地持久化

{{UserDefaults/数据库/文件等持久化键值列表与用途}}

## 边界约定

{{数据访问边界，如：}}
- {{UI 层不直接构造/解析底层请求响应}}
- {{列表只读轻量摘要，不加载完整数据}}
- {{分页策略与全量加载限制}}
