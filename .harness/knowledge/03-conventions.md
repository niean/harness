# 约定与约束（实现细节）

本文件是项目规范约定的权威来源，AGENTS.md "二、项目规范"各节为摘要引用，以本文件为准。

---

# 一、UI交互约定

{{项目 UI 交互约定，如：}}
- {{手势规范}}
- {{导航规范}}
- {{页面重置/销毁规范}}

---

# 二、编码约定

{{按项目实际填写编码约定，可包含以下子节：}}

## 资源处理

{{如图片尺寸规范、音频处理规范等}}

## 特殊值处理

{{如保留字、失败返回值、索引对应关系等}}

## 本地化

{{开发语言、支持的地区、本地化策略}}

## 常量

{{常量管理方式、分类规则、新增规范}}

## 禁止 Mock 造假

生产代码禁止以硬编码假数据冒充真实实现。具体规则：

- 禁止返回硬编码零值/固定值伪装为真实采集结果
- 系统 API 不可用或受限时，必须标注不可用原因（注释 `// [Unavailable]` + 原因），返回值或 UI 体现"不可用"状态（如 "N/A"、nil），禁止静默返回零值
- Stub/占位实现必须满足：(1) 函数名或注释包含 `stub`/`placeholder`；(2) 运行时日志输出提示；(3) 返回 Optional nil 或专用枚举 case，禁止返回业务合法值

---

# 三、质量约定

## 编译

零警告（含 IDE 配置警告、工具级警告），提交前构建验证。

## 错误处理

- 用户：本地化自然语言，无技术细节
- 开发：日志含错误码，禁止完整密钥
- 异步错误切回主线程再更新 UI
- 超时：必须设超时，不允许无超时请求
{{项目特有错误处理模式}}

## 日志

{{日志框架、分类定义位置、级别说明、格式约束}}

## 线程与并发

{{线程使用规范、并发框架选择、取消机制}}

## 内存与性能

{{缓存策略、解码规范、列表性能、存储分离等}}

## 单元测试

{{测试目录、命名规范、Mock 策略、端到端测试说明}}

---

# 四、文件管理约定

- 禁止主动创建 README（包括为新建目录添加 README.md 说明文件）
- 不删除项目文件
- 文件名：小写英文 kebab-case，动词-名词语序
- 知识库编号分段：同一目录下按文件性质分段编号（如 knowledge/ 01~05 认知约束类、21~22 工具索引类），新增文件归入对应段的下一个序号
- 执行计划文件落盘到 `.harness/plans/active/plan-{YYMMDD}-{desc}.md`，任务完成后移动到 `completed/`
- 命令行超过 10 行时，必须先将脚本写入 `locals/harness_tmp/` 再执行；AI 自主维护该目录

---

# 五、安全约定

{{项目安全约定，如：}}
- 密钥：{{安全存储方式、键名管理、配置文件策略}}
- 网络：{{协议要求、响应校验、数据脱敏}}
- 隐私：{{数据存储位置、外发数据范围、导出机制}}
