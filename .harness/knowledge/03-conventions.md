<!-- SUMMARY: {{概述该文件内容的一句话}} -->
# 约定与约束（实现细节）

本文件是项目规范约定的权威来源，`.harness/PROJECT.md` "项目规范"各节为摘要引用，以本文件为准。

---

# 一、UI交互约定

- 手势：{{手势交互规则，如滑动返回的触发条件、注释标记、参数来源}}
- Tab 重置：{{Tab重置策略，哪些Tab参与重置、重置机制}}

---

# 二、编码约定

## 图片尺寸

- 入队：{{入队时的图片降采样规则与API}}
- 播放：{{播放时的按需加载尺寸与预加载策略}}
- 头像：{{头像保存时的尺寸与格式}}
- {{外发API的图片约束}}

## {{编码约定类别3}}

{{该类别的约定规则列表}}

## 本地化

- {{开发区域与已知区域配置}}
- {{系统集成的语言模型生成策略}}

## 字体

- {{字体统一引用规则与禁止项}}
- {{字体分类：语义字体/自适应字体/固定字体的定义方式}}
- {{视图私有动态计算字体的例外规则}}
- {{新增字体的复用与命名规范}}

## 常量

- {{常量文件路径与已有分类列表}}
- {{新增常量的归类规则}}
- {{禁止硬编码魔法值}}
- {{运行时可变配置的管理方式}}

## 禁止 Mock 造假

生产代码禁止以硬编码假数据冒充真实实现。具体规则：

- {{禁止返回硬编码零值/固定值伪装真实结果的规则}}
- {{系统API不可用时的标注与返回值规则}}
- {{Stub/占位实现的必要条件}}
- {{Code Review对采集/监控/统计类函数的检查要求}}

---

# 三、质量约定

## 编译

{{零警告要求、构建验证命令、特殊构建参数}}

## 错误处理

- 用户：{{用户侧错误提示规则}}
- 开发：{{开发侧日志规则与敏感信息处理}}
- {{异步错误的线程切换规则}}
- 超时：{{默认超时与大文件超时配置}}
- 错误分层模式：{{服务级错误枚举的实现规范，包括双属性要求}}
- {{NSError创建的统一规范}}

## 日志

- {{日志API约束（禁止print等）}}
- {{日志分类定义位置与subsystem}}
- 级别：{{日志级别列表}}
- {{日志文本格式约束}}

## 线程与并发

- {{UI更新与后台任务的线程分配}}
- {{各层的并发模型（Concurrency/Combine等）}}
- 取消：{{任务取消机制}}

## 内存与性能

{{图片缓存策略：NSCache配置、缓存键格式、预加载规则}}

{{图片解码约束：解码方式、禁止全尺寸解码的原因}}

{{列表性能：metadata分离加载、头像加载规则}}

{{存储分离：大对象独立存储、加载时重组方式}}

{{调试日志：存储方式与读取限制}}

## 单元测试

- {{测试目录与命名规则}}
- {{补充测试的触发条件}}
- {{外部依赖的Mock方式}}
- {{端到端测试的CI策略}}

## 代码扫描

代码扫描（Reviewer 代码扫描能力）发现的问题处理规则：

- 本次变更新引入的问题：{{处理方式}}
- 既存问题（非本次引入）：{{处理方式}}
- 判定方法：{{新引入与既存问题的判定规则}}
- 例外情况：{{无论是否本次引入都需立即修复的情况}}

---

# 四、文件管理约定

- {{禁止主动创建README的规则}}
- {{禁止自主删除项目文件的规则与例外}}
- {{文件命名规范}}
- {{知识库编号分段规则}}
- {{执行计划文件的存放与归档路径}}
- {{长命令的脚本化执行规则}}

---

# 五、安全约定

- 密钥：{{密钥存储方式与禁止项}}
- 网络：{{HTTPS强制、响应校验、图片外发约束}}
- 隐私：{{数据存储位置、外发数据范围、导出方式、配置文件保护}}
