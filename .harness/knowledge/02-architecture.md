# 架构与模块边界

## 分层

{{按项目实际描述分层架构，示例：}}
- 表现层（{{路径}}）：{{UI 框架与主要视图}}
- 协调层（{{路径}}）：{{业务编排与流程串联}}
- 能力层（{{路径}}）：{{服务、管理器列表与职责}}
- 系统集成（{{路径}}）：{{系统级集成说明}}
- 数据层（{{路径}}）：{{数据模型与配置}}

## 模块边界

{{列举关键模块边界约束，如：}}
- {{UI 不直接调用底层 API，通过 Coordinator}}
- {{跨界面状态集中管理方式}}
- {{模块间通信机制}}

## 关键约束

{{列举架构关键约束，如：}}
- {{全屏页面管理方式}}
- {{导航结构说明}}
- {{数据边界：数据模型为权威结构，UI 层不解析原始格式}}
