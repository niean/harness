# 架构与模块边界

## 分层结构

{{描述项目的分层架构，例如：}}
{{- UI 层：视图组件}}
{{- 业务协调层：协调器/控制器}}
{{- 服务层：外部API调用}}
{{- 数据管理层：数据持久化与管理}}

## 模块依赖

{{描述模块间的依赖关系和调用方向，例如：}}
{{- UI -> 协调器 -> 服务（单向依赖，UI层不直接调用服务层）}}

## 关键边界约束

{{列出需要严格遵守的架构边界，例如：}}
{{- 哪些层之间不允许直接调用}}
{{- 全局状态的管理方式和访问规则}}
{{- 模块间通信的标准方式}}
